# Cleanup Report - NeoNatal Watch AI

| File/Folder | Reason | Safe to delete | Dependency check |
| --- | --- | --- | --- |
| `scripts/create_notebook*.py` | Generated early-phase notebooks dynamically. Obsolete as notebooks are established. | Yes | Not imported or used in codebase. |
| `scripts/run_phase*.py` | Intermediate execution scripts for testing specific phases. Unused and obsolete. | Yes | Not imported or used in codebase. |
| `__pycache__/` (all occurrences) | Generated Python bytecode cache. | Yes | Auto-regenerated by Python. |
| `.pytest_cache/` | Test runner cache. | Yes | Auto-regenerated by Pytest. |

**Important Note on Kept Data**:
- `test_neonatal.db` & `neonatal.db` are local databases but are required for local testing and running the backend prototype, respectively. They were added to `.gitignore` so they aren't committed, but they are NOT deleted from disk.
- All trained models (`models/*.keras`, `models/*.json`) and synthetic datasets (`data/synthetic/`) are absolutely required for the system to run and serve inferences. They are retained.

